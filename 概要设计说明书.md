# 概要设计说明书

## 1 引言

### 1.7 v1.2（2026-04-28）架构与流程更新
- 管理端订单聚合逻辑：`adminListAllWorkOrders` 以 `jobs` 记录为主返回列表，状态展示与筛选按岗位维度执行。
- 订单详情接口扩展：新增岗位维度详情能力（支持招募中岗位无工单场景）。
- 纠纷处理扩展：管理员支持“押金原路返回 + 人工裁定 + 驳回”，并以订单级押金进行校验。
- 状态展示规则修正：纠纷处理结果区分“已完成”与“已驳回”。
- 前端提示统一：申诉提交成功后统一弹窗反馈。

### 1.1 目的
本文档用于描述“校园兼职工分管理小程序”当前实现的总体设计方案，覆盖微信小程序前端、云函数后端与云数据库结构，为后续开发、测试、运维和课程文档提交提供统一依据。

### 1.2 范围

#### 1.2.1 系统目标
- 提供兼职者、发布者、管理员三类角色的业务闭环：发布岗位、报名录用、签到、完工提交、结算、投诉处理与审核。
- 以“工分”为统一结算单位，支持账户充值、提现及流水查询。
- 通过云函数集中实现业务规则，前端页面负责交互与状态展示。

#### 1.2.2 主要软件需求
- 微信登录后自动初始化用户记录与默认角色。
- 发布者可发布岗位、查看报名、录用学生、生成签到码、处理工单。
- 兼职者可浏览岗位、报名、输入签到码签到、提交完工文字与照片、发起申诉与评价。
- 管理员可审核用户、审核岗位、查看订单详情、处理纠纷。
- 内置聊天能力，支持基于岗位上下文的一对一消息沟通。
- 支持基于地点、薪资、空闲时间的岗位推荐能力，支持地点选择器辅助定位。

#### 1.2.3 软件设计约束、限制
- 运行环境固定为微信小程序 + 微信云开发（Cloud Functions / Cloud Database）。
- 鉴权依赖 `OPENID`，不采用自建账号密码体系。
- 纠纷、订单状态流转受云函数中的状态机约束（非法状态会被拒绝）。
- 文件存储（完工图/申诉图）依赖 `wx.cloud.uploadFile`，需要云存储可用。

### 1.3 缩写
- MP：Mini Program（微信小程序）
- CF：Cloud Function（云函数）
- DB：Cloud Database（云数据库）
- UI：User Interface（用户界面）
- API：Application Programming Interface（接口）

### 1.4 术语
- 工分：平台内结算与账户增减的积分单位。
- 岗位：发布者发布的兼职需求实体（`jobs`）。
- 工单/订单：学生被录用后形成的执行单据（`work_orders`）。
- 申诉/纠纷：围绕工单产生的投诉记录（`disputes`）。
- 签到码：发布者生成、学生输入的 6 位动态验证码。

### 1.5 参考资料
- 微信小程序官方文档（框架、WXML/WXSS/JS）
- 微信云开发官方文档（云函数、数据库、云存储）
- 项目源码：`app.json`、`pages/**`、`cloudfunctions/**`

### 1.6 版本信息
- 文档版本：v1.2
- 修订时间：2026-04-24
- 对应代码基线：当前工作区 `f:\ruanjiangongcheng`（以现有文件为准）

---

## 2 数据设计

### 2.1 数据对象和形成的数据结构
核心数据对象如下：
- 用户（Users）：基础资料、角色、认证状态、账户余额、管理员标识。
- 岗位（Jobs）：兼职需求描述、人数、状态、押金和托管工分信息。
- 报名（Applications）：学生对岗位的投递记录。
- 工单（WorkOrders）：录用后执行单，含签到、完工、结算、互评信息。
- 纠纷（Disputes）：投诉/申诉记录，含处理状态、管理员备注、结果。
- 消息（Messages）：聊天消息流，按岗位 + 双方 openid 归并会话。
- 钱包流水（WalletLogs）：充值/提现明细。
- 信用流水（CreditLogs）：评分与履约行为导致的信用分变更记录。
- 爽约记录（OrderReports）：爽约处理留痕。

### 2.2 文件和数据库结构

#### 2.2.1 外部文件结构
##### ① 逻辑结构
- 前端文件：`pages/<模块>/<页面>.{js,wxml,wxss,json}`
- 云函数文件：`cloudfunctions/<函数名>/index.js`
- 全局页面注册：`app.json`
- 云端图片文件（逻辑路径）：
  - 完工图：`work-proof/<orderId>/<timestamp>.jpg`
  - 申诉图：`appeal-proof/<orderId>/<timestamp>.jpg`

##### ② 逻辑记录描述
- 完工图记录字段：`work_orders.submitPhotoFileId`
- 申诉图记录字段：`disputes.disputePhotoFileId`
- 文件记录与工单/纠纷通过 `orderId` 关联。

##### ③ 访问方法
- 前端通过 `wx.cloud.uploadFile` 上传，获得 `fileID`。
- 通过云函数写入 DB 字段；页面直接以 `fileID` 作为 `image src` 展示。

#### 2.2.2 全局数据
项目主要数据库集合（由云函数 `ensureCollection` 保障）：
- `users`
- `jobs`
- `applications`
- `work_orders`
- `messages`
- `credit_logs`
- `order_reports`
- `disputes`
- `wallet_logs`

#### 2.2.3 文件和数据交叉索引
- `jobs._id` <-> `applications.jobId`、`work_orders.jobId`、`messages.jobId`
- `work_orders._id` <-> `disputes.orderId`、`order_reports.orderId`
- `users.openid` <-> 各集合中的 `studentOpenid/publisherOpenid/complainantOpenid/respondentOpenid/fromOpenid/toOpenid`
- `work_orders.submitPhotoFileId` 与云存储完工图片一一对应
- `disputes.disputePhotoFileId` 与云存储申诉图片一一对应

---

## 3 体系结构设计

### 3.1 数据和控制流复审
- 表现层：小程序页面（`pages/**`）负责交互、输入校验与展示。
- 业务层：云函数（`job/user/login/signIn/generateSignCode`）负责权限、状态机、事务性更新。
- 数据层：云数据库集合保存业务状态；云存储保存图片证据。
- 典型流程：
  1. 登录 -> `login` 云函数创建/更新用户
  2. 发布岗位 -> `job:createJob`
  3. 报名录用 -> `applyJob` + `acceptApplication` 生成工单
  4. 签到 -> `generateSignCode` + `signIn`
  5. 完工 -> `submitWork` -> `confirmWork`
  6. 纠纷 -> `report*` -> `adminResolveDispute`

### 3.2 得出的程序结构
- 前端按角色分层：
  - 兼职者：`student/*`
  - 发布者：`publisher/*`
  - 管理员：`admin/*`
  - 公共能力：`location-picker/*`（地点选择）、`student/recommend/*`（推荐岗位）
- 后端按领域分层：
  - 账户/认证：`user`
  - 兼职业务主域：`job`
  - 签到码与签到：`generateSignCode`、`signIn`
  - 登录：`login`

---

## 4 界面设计

### 4.1 人机界面规约
- 统一卡片式布局、圆角和阴影风格。
- 角色页面底部采用 tab 样式导航（部分子页去除底部导航避免页面栈循环）。
- 关键动作（结算、投诉、审核）采用显性按钮并二次确认（`showModal`）。

### 4.2 人机界面设计规约
- 订单状态、筛选标签使用椭圆样式和颜色区分。
- 图片型证据在详情页直接可见，减少跳转成本。
- 表单输入采用“必填校验 + Toast 提示 + Loading”交互模式。
- 审核流（用户、岗位、纠纷）均支持即时反馈并刷新列表。
- 状态类字段采用“前端映射展示、数据库保留英文值”的策略：
  - 身份：`student/publisher/admin` 映射为中文角色名；
  - 认证状态：`approved/pending/rejected/none|unverified` 映射中文；
  - 岗位/订单状态：`open/ongoing/closed/...` 映射中文。

### 4.4 内部接口设计规约
- 前端统一通过 `utils/cloud.js` 的 `call(functionName, payload)` 调用云函数。
- `payload.action` 用于在函数内路由业务分支（尤其 `job`、`user`）。
- 响应约定：
  - 成功：`{ code: 0, ... }`
  - 失败：`{ code: 非0, message: '错误信息' }`

---

## 5 接口设计

### 5.1 外部接口设计

#### 5.1.1 外部数据接口
- 微信云数据库接口（集合 CRUD）
- 微信云存储接口（图片上传/读取）
- 微信小程序系统接口（`wx.showModal`、`wx.navigateTo`、`wx.chooseMedia` 等）

#### 5.1.2 外部系统或设备接口
- 本项目不涉及硬件设备接口。
- 本项目不涉及第三方企业系统对接接口。

### 5.2 内部接口设计

#### 5.2.1 内部模块接口调用关系
- 前端页面 -> 云函数：
  - 用户域：`user`（资料、认证、钱包、管理员审核用户）
  - 业务域：`job`（岗位、工单、聊天、纠纷、管理员订单）
  - 签到域：`generateSignCode`、`signIn`
  - 登录域：`login`
- 云函数内部：
  - `job` 通过 `getUser/getJob` 获取关联实体
  - `job` 通过 `performOrderCompletion/performNoShowSettlement` 执行结算逻辑
- 前端展示层：
  - 统一由 `utils/status-text.js` 承担状态与角色文案映射，避免页面重复硬编码。

#### 5.2.2 接口数据结构
- 通用请求结构：
  - `action: string`
  - 业务参数：如 `jobId/orderId/disputeId/amount/text/reason/...`
- 通用响应结构：
  - `code: number`
  - `message?: string`
  - `data/list/user/job/order/disputes...`（按 action 返回）
- 典型接口示例：
  - `job:submitWork`：`{orderId, photoFileId, text}`
  - `job:reportMerchantNoPay`：`{orderId, reason, photoFileId?}`
  - `job:adminResolveDispute`：`{disputeId, approve, adminNote}`

---

## 6 模块过程设计

### 6.1 处理说明
- 登录模块：
  - 首次登录创建用户，默认 `student` 身份，初始化余额与认证状态。
- 岗位模块：
  - 发布时扣发布者押金，岗位初始 `pending_review`，经管理员审核后开放。
- 工单模块：
  - 录用生成工单并扣学生押金；
  - 签到后写入 `signTime` 并更新状态；
  - 完工提交需文字+照片，进入待结算；
  - 结算完成返还押金并发放工分。
- 纠纷模块：
  - 学生/发布者可发起纠纷（按状态与权限限制）；
  - 管理员“接受/驳回”处理后状态收敛为已完成展示。

### 6.2 接口描述
- 见第 5 章接口设计，核心在 `job` 云函数 action 路由。

### 6.3 设计语言描述
- 前端：JavaScript + WXML + WXSS
- 后端：Node.js（微信云函数）
- 数据：微信云数据库（文档型）

### 6.4 使用的模块
- 角色认证与资料模块
- 钱包与工分模块
- 岗位发布与审核模块
- 报名录用模块
- 工单签到与完工模块
- 纠纷申诉与处理模块
- 聊天消息模块

### 6.5 内部设计结构
- `job` 云函数是业务中枢，按 action 分段实现状态驱动。
- 页面按角色拆分，管理员端拆分为审核中心、订单管理、纠纷处理、订单详情等。
- 数据模型围绕 `users/jobs/work_orders/disputes` 形成主干。

### 6.6 注释/约束/限制
- 多数关键接口依赖角色与认证状态校验（`assertApprovedStudent/Publisher/Admin`）。
- 状态迁移受 `ORDER_STATUS_TRANSITIONS` 与 `JOB_STATUS_TRANSITIONS` 限制。
- 投诉并发控制：同一工单存在 `pending` 纠纷时禁止重复提交。

---

## 7 需求交叉索引
- 用户登录/资料维护 -> `login`、`user:getProfile/updateProfile`
- 身份切换与认证 -> `user:setRole/submitVerify/adminApproveUser/adminRejectUser`
- 工分账户 -> `user:recharge/withdraw/walletLogs`
- 岗位发布与审核 -> `job:createJob/adminListPendingJobs/adminApproveJob/adminRejectJob`
- 报名与录用 -> `job:applyJob/listApplications/acceptApplication`
- 工单执行 -> `generateSignCode`、`signIn`、`job:submitWork/confirmWork`
- 投诉与纠纷 -> `job:reportNoShow/reportMerchantNoPay/reportOtherDispute/adminResolveDispute`
- 订单管理与详情 -> `job:adminListAllWorkOrders/adminGetWorkOrderDetail`
- 聊天沟通 -> `job:sendMessage/listMessages/listMyChats`
- 岗位推荐与距离评分 -> `job:getRecommendedJobs`、`pages/student/recommend/recommend`、`pages/location-picker/location-picker`

---

## 7A 答辩展示要点（课程提交优化）

### 7A.1 业务闭环流程图（文字版）
```text
微信登录 -> 用户初始化/角色确认
   ->（发布者）发布岗位 -> 管理员审核岗位
   ->（兼职者）浏览/推荐岗位 -> 报名
   ->（发布者）录用 -> 生成工单
   ->（发布者）生成签到码 ->（兼职者）签到
   ->（兼职者）提交完工（图+文）
   ->（发布者/管理员）确认完工 -> 工分结算
   -> 双方评价（信用分变更）
   -> 异常分支：发起纠纷 -> 管理员处理 -> 状态收敛
```

### 7A.2 关键状态流转表

| 对象 | 状态流转（主路径） | 异常/收敛路径 |
|---|---|---|
| 用户认证（`users.verifyStatus`） | `none -> pending -> approved` | `pending -> rejected` |
| 岗位（`jobs.status`） | `pending_review -> open -> ongoing -> closed` | `pending_review -> rejected`；`open/ongoing -> publisher_cancelled/admin_cancelled` |
| 工单（`work_orders.status`） | `ongoing -> pending_settlement/submitted -> completed` | `ongoing/pending_settlement/submitted -> closed`；`-> publisher_cancelled/admin_cancelled` |
| 纠纷（`disputes.status`） | `pending -> resolved` | 处理结果通过 `resolveResult=approved/rejected` 区分 |

### 7A.3 接口汇总表（按答辩常见问法）

| 能力域 | 关键云函数 action | 前端入口页 |
|---|---|---|
| 登录与身份初始化 | `login.main` | `pages/login/login` |
| 用户资料与角色切换 | `user:getProfile/updateProfile/setRole/switchBackAdmin` | `pages/index/index`、`pages/*/mine`、`pages/profile/edit` |
| 用户认证与管理员审核 | `user:submitVerify/adminListPendingUsers/adminApproveUser/adminRejectUser` | `pages/register/*`、`pages/admin/users` |
| 岗位发布与审核 | `job:createJob/adminListPendingJobs/adminApproveJob/adminRejectJob` | `pages/publisher/create`、`pages/admin/pending-jobs` |
| 报名与录用 | `job:applyJob/listApplications/acceptApplication` | `pages/student/jobs`、`pages/publisher/job-detail` |
| 签到与完工结算 | `generateSignCode.main`、`signIn.main`、`job:submitWork/confirmWork` | `pages/student/orders`、`pages/work/submit`、`pages/publisher/job-detail` |
| 纠纷与申诉处理 | `job:reportNoShow/reportMerchantNoPay/reportOtherDispute/adminResolveDispute` | `pages/student/orders`、`pages/publisher/job-detail`、`pages/admin/disputes` |
| 订单管理 | `job:adminListAllWorkOrders/adminGetWorkOrderDetail` | `pages/admin/orders`、`pages/admin/order-detail` |
| 聊天沟通 | `job:sendMessage/listMessages/listMyChats` | `pages/student/contact`、`pages/publisher/contact`、`pages/chat/detail` |
| 推荐与定位 | `job:getRecommendedJobs` | `pages/student/recommend/recommend`、`pages/location-picker/location-picker` |

### 7A.4 状态展示口径（前端中文化）
- 文档答辩口径：数据库保留英文状态值，前端统一映射中文展示；
- 映射实现位置：`utils/status-text.js`；
- 覆盖范围：角色、审核状态、岗位/订单状态，避免页面硬编码不一致。

---

## 8 测试部分

### 8.1 测试方针
- 以“角色 + 状态机 + 资金/积分变化”为主线开展功能测试。
- 覆盖正常流、越权流、非法状态流、重复提交流、异常回退流。

### 8.2 集成策略
- 分层联调：
  1. 先验证云函数 action 返回与数据落库；
  2. 再验证页面调用链与展示；
  3. 最后执行端到端场景（发布->录用->签到->完工->结算->评价/纠纷）。
- 对关键分支进行回归：
  - 签到后提交完工
  - 纠纷接受/驳回状态展示
  - 管理员订单详情跳转纠纷处理

### 8.3 特殊考虑
- 云函数版本与前端版本不一致时，可能出现 `未知 action`；需同步部署。
- 图片上传依赖网络与云存储权限，需验证“上传失败”分支提示。
- 管理员/普通用户角色切换需验证缓存一致性（`wx.setStorageSync('user')`）。

---

## 9 附录（包括特殊注解）
- 外部设备接口：本项目不涉及。
- 第三方硬件驱动：本项目不涉及。
- 第三方支付网关：当前为模拟充值提现（`channel: 'mock'`），未接真实支付。
- 文档中“5.1.2 接口数据结构”按要求已按“5.2.2”处理。

