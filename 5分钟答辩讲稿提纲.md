# 5分钟答辩讲稿提纲

> 主题：校园兼职工分管理小程序  
> 结构：问题 -> 方案 -> 实现 -> 结果 -> 亮点  
> 建议总时长：5分钟

## v1.2 答辩补充要点（2026-04-28）

- 管理端订单管理已从工单视角升级为岗位状态视角，状态展示与业务一致。
- 纠纷处理不再是简单接受/驳回，已支持“押金原路返回 + 人工裁定分配”。
- 人工裁定金额校验采用订单级总押金口径，避免岗位总人数造成的误判。
- 用户侧申诉成功反馈统一为弹窗“申诉订单已上传”。

---

## 0. 开场（约20秒）

各位老师好，我的课设项目是**校园兼职工分管理小程序**。  
我今天从五个部分汇报：**问题、方案、实现、结果、亮点**，重点说明这个系统如何完成“发布岗位到结算评价”的完整闭环。

---

## 1. 问题（约50秒）

我们在校园兼职场景里，主要面临四类问题：

1. **流程割裂**：发布、报名、录用、签到、结算分散在不同工具里，效率低。  
2. **信任不足**：兼职方担心“做了不结算”，发布方担心“接单不到岗”。  
3. **管理困难**：没有统一审核与纠纷处理机制，出现问题难追责。  
4. **信息匹配粗糙**：岗位和学生匹配主要靠人工筛选，成本高、效果一般。

所以我们希望做一个系统，既能支撑日常流程，也能处理异常情况，并且具备一定智能推荐能力。

---

## 2. 方案（约60秒）

针对这些问题，我设计了一个**三角色 + 状态机 + 云函数集中业务**的方案：

1. **三角色分工**：兼职者、发布者、管理员，职责清晰。  
2. **状态机约束**：岗位、工单、纠纷都按合法状态迁移，防止越权和脏数据。  
3. **资金与规则后移**：押金、结算、信用分变化都放在云函数，不放前端。  
4. **异常闭环**：支持投诉/申诉，管理员审核后统一收敛。  
5. **推荐增强**：基于距离、薪资、时间匹配做岗位推荐。

核心目标是：**正常流程可跑通，异常流程可收敛，关键数据可追溯**。

---

## 3. 实现（约90秒）

### 3.1 技术实现

- 前端：微信小程序（页面按角色拆分）  
- 后端：微信云函数（`login/user/job/signIn/generateSignCode`）  
- 数据：云数据库 + 云存储（完工图、申诉图）

### 3.2 业务主链路

1. 微信登录后初始化用户；  
2. 发布者发布岗位，管理员审核；  
3. 兼职者浏览/推荐列表后报名；  
4. 发布者录用，生成工单并进入执行；  
5. 发布者生成签到码，兼职者签到；  
6. 兼职者提交完工图文；  
7. 发布者或管理员确认完工并结算；  
8. 双方评价，信用分更新。

### 3.3 异常链路

- 若出现爽约、拒不结算、其他争议，可发起纠纷；  
- 管理员处理后写入结果并完成状态收敛；  
- 同一订单待处理纠纷做了并发限制，避免重复提交。

---

## 4. 结果（约60秒）

项目完成后达到了四个结果：

1. **功能完整**：实现了从发布到结算再到评价/纠纷的闭环。  
2. **角色清晰**：学生、发布者、管理员页面和权限分离明确。  
3. **数据一致**：通过状态迁移控制，减少非法操作。  
4. **体验提升**：前端状态统一中文映射展示，用户可读性更好；推荐岗位提升筛选效率。

从测试角度看，主流程、越权流程、异常流程都能得到对应处理和提示。

---

## 5. 亮点（约50秒）

我认为这个项目有四个亮点：

1. **状态机驱动业务**：岗位、工单、纠纷都有可控迁移路径。  
2. **云函数集中规则**：押金、结算、信用等核心逻辑不放前端，安全性更高。  
3. **展示与存储解耦**：数据库保留英文状态，前端统一中文映射，便于兼容扩展。  
4. **推荐模块可降级**：支持第三方地图距离，失败时回退本地算法，保证可用性。

---

## 6. 结束语（约10秒）

以上是我的汇报。  
这个项目重点解决了校园兼职中的流程、信任和管理问题，并在可维护性和可扩展性上做了针对性设计。谢谢各位老师。

---

## 附：答辩追问快速应答（可选）

- **问：为什么用状态机？**  
  因为兼职业务是强流程场景，状态机能把非法操作挡在接口层，保证数据一致性。

- **问：为什么押金和结算不放前端？**  
  前端可被篡改，资金与信用相关逻辑必须后端统一执行。

- **问：推荐算法为什么这么设计？**  
  先用“距离+薪资+时间”做可解释的轻量方案，后续可替换成更复杂模型。

- **问：系统还能扩展什么？**  
  可扩展真实支付、消息已读回执、推荐模型迭代、管理端数据报表。
