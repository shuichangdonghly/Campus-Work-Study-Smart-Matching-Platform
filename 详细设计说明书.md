# 详细设计说明书

## 1 引言

### 1.5 v1.2（2026-04-28）详细实现更新
- 管理员订单页：按 `jobs.status` 分类展示岗位订单视图，保留申诉筛选标签与申诉内容。
- 管理员订单详情：支持通过 `orderId/jobId` 打开；取消交易后按钮灰显且禁用。
- 纠纷处理页：支持“押金原路返回 / 驳回 / 人工裁定”三类处理动作。
- 人工裁定弹窗：输入“给兼职方金额”“给发布方金额”，并校验两者和等于当前订单总押金。
- 押金口径：按订单级押金处理，不按岗位总名额推导；文案与页面展示同步（学生押金、发布方押金、订单总押金）。

### 1.1 编写目的
本文档在《概要设计说明书》基础上，进一步给出项目的详细实现设计，覆盖云函数、前端页面、核心数据结构与模块级实现细节，用于：
- 指导开发与迭代；
- 支撑测试用例设计；
- 作为课程设计答辩与交付材料。

### 1.2 项目背景
本项目为微信小程序兼职管理系统，围绕“发布岗位—报名录用—签到—完工提交—结算—纠纷处理”全流程设计，参与角色包括：
- 兼职者（student）
- 发布者（publisher）
- 管理员（admin）

系统采用微信云开发架构：小程序前端 + 云函数后端 + 云数据库 + 云存储。

### 1.3 定义
- 工分：平台内部结算积分单位。
- 岗位：发布者发起的兼职需求（`jobs`）。
- 工单/订单：学生被录用后形成的执行单（`work_orders`）。
- 纠纷：围绕工单的投诉/申诉记录（`disputes`）。
- 签到码：发布者生成的 6 位码，学生输入后完成签到。

### 1.4 参考资料
- 微信小程序开发文档
- 微信云开发文档（Cloud Functions / Cloud Database / Cloud Storage）
- 项目源码：
  - `app.json`
  - `pages/**`
  - `cloudfunctions/**`
  - `utils/cloud.js`

---

## 2 总体设计

### 2.1 需求概述
系统核心需求包括：
- 微信登录与用户资料管理；
- 角色选择、认证申请与管理员审核；
- 岗位发布、岗位审核、报名录用；
- 工单状态流转（进行中、签到后、待结算、已完成）；
- 签到码生成与验证；
- 完工文字+图片提交；
- 纠纷申诉提交与管理员处理；
- 工分账户充值、提现、流水；
- 岗位上下文聊天沟通。
- 推荐岗位能力（距离/薪资/时间综合评分）与地点选择能力。

### 2.2 软件结构（文字结构图）
```text
小程序前端（pages）
├─ 登录/首页/个人资料
├─ 兼职者端（jobs/contact/job-detail/mine/orders）
├─ 推荐与定位（student/recommend, location-picker）
├─ 发布者端（create/jobs/contact/job-detail/mine）
├─ 管理端（audit/users/pending-jobs/orders/order-detail/disputes/mine）
├─ 钱包（index/recharge/withdraw）
└─ 聊天详情（chat/detail）

云函数后端（cloudfunctions）
├─ login            # 登录与用户初始化
├─ user             # 用户资料、认证、钱包、管理员审核用户
├─ job              # 岗位/工单/纠纷/消息/管理员订单主业务
├─ generateSignCode # 生成签到码
└─ signIn           # 学生签到

数据层（Cloud DB）
├─ users / jobs / applications / work_orders
├─ disputes / order_reports / credit_logs
├─ wallet_logs / messages
└─ 云存储：完工图、申诉图
```

---

## 3 程序描述

> 先给出模块清单，再逐个详细描述。  
> 说明：以下模块已覆盖项目中所有主要云函数和前端页面逻辑。

### 3.0 模块清单
1. 3.1 登录与用户初始化模块（`cloudfunctions/login`，`pages/login/login`，`pages/index/index`）
2. 3.2 用户资料与角色管理模块（`cloudfunctions/user`，`pages/profile/edit`，`pages/register/*`，`pages/*/mine`，`utils/status-text.js`）
3. 3.3 钱包与工分模块（`cloudfunctions/user` 的钱包 action，`pages/wallet/*`）
4. 3.4 岗位发布与审核模块（`cloudfunctions/job` 岗位 action，`pages/publisher/create`，`pages/admin/pending-jobs`）
5. 3.5 报名与录用模块（`cloudfunctions/job` 的 `applyJob/listApplications/acceptApplication`，`pages/student/jobs`，`pages/publisher/job-detail`）
6. 3.6 工单执行与完工提交模块（`cloudfunctions/job:myWorkOrders/submitWork/confirmWork`，`cloudfunctions/generateSignCode`，`cloudfunctions/signIn`，`pages/student/orders`，`pages/work/submit`）
7. 3.7 纠纷申诉与处理模块（`cloudfunctions/job:report*/adminResolveDispute/adminListPendingDisputes`，`pages/admin/disputes`，`pages/admin/order-detail`）
8. 3.8 订单管理模块（`cloudfunctions/job:adminListAllWorkOrders/adminGetWorkOrderDetail`，`pages/admin/orders`，`pages/admin/order-detail`）
9. 3.9 聊天通信模块（`cloudfunctions/job:sendMessage/listMessages/listMyChats`，`pages/student/contact`，`pages/publisher/contact`，`pages/chat/detail`）
10. 3.10 评价与信用模块（`cloudfunctions/job:submitReview`，订单详情与工单页评价入口）
11. 3.11 管理员审核中心聚合模块（`pages/admin/audit`，跳转 `users/pending-jobs/orders`）
12. 3.12 推荐岗位与定位模块（`cloudfunctions/job:getRecommendedJobs`，`pages/student/recommend/recommend`，`pages/location-picker/location-picker`）

---

### 3.1 登录与用户初始化模块

#### 3.1.1 性能
- 登录与初始化在单次云函数调用内完成，数据库读写量低（1~3 次）。
- 首次登录可在秒级完成用户创建。

#### 3.1.2 输入项目
- 微信上下文 `OPENID`
- 可选 `userInfo`（昵称、头像）

#### 3.1.3 功能
- 若用户不存在则创建默认记录（student、未认证、工分 0）。
- 若用户存在则补齐空字段并刷新 `updateTime`。
- 支持管理员 bootstrap openid 自动晋升管理员。

#### 3.1.4 输出项目
- `openid`
- `user`（完整用户对象）
- `code/message`

#### 3.1.5 算法
- 无特殊算法；采用条件分支 + 幂等写入。

#### 3.1.6 程序逻辑
1. 校验 `OPENID`；
2. `ensureCollection('users')`；
3. 查询用户是否存在；
4. 不存在则新增；存在则差量更新；
5. 管理员条件处理；
6. 返回用户信息。

#### 3.1.7 接口
- 云函数：`login.main(event)`
- 前端调用点：`pages/login/login`、`pages/index/index`（进入前检查）

#### 3.1.8 限制条件
- 依赖微信云环境；本地无法脱离云函数运行。

#### 3.1.9 存储分配
- 集合：`users`

#### 3.1.10 测试要点
- 首次登录创建用户；
- 非首次登录不覆盖手工资料；
- bootstrap 管理员自动生效。

---

### 3.2 用户资料与角色管理模块

#### 3.2.1 性能
- 单次资料查询/更新 O(1)，主要为单表操作。

#### 3.2.2 输入项目
- `action`：
  - `getProfile/getPublicProfile/updateProfile`
  - `setRole/switchBackAdmin`
  - `submitVerify/adminListPendingUsers/adminApproveUser/adminRejectUser/adminFallbackToStudent`
- 对应参数：`payload/targetOpenid/applyRole/reason`

#### 3.2.3 功能
- 读取本人资料、读取他人公开资料；
- 更新昵称头像简介；
- 角色切换并同步认证状态；
- 提交认证申请（student/publisher）；
- 管理员审核通过/驳回用户认证。
- 统一状态中文展示映射：身份、认证状态、岗位/订单状态在前端转换，不改库内英文值。

#### 3.2.4 输出项目
- `user` 或 `list`
- 审核结果状态码

#### 3.2.5 算法
- 无特殊算法；以角色状态机与字段映射为主。

#### 3.2.6 程序逻辑
1. 读取当前用户 `me`；
2. 根据 `action` 路由；
3. 进行管理员权限或参数校验；
4. 写入用户状态字段；
5. 返回最新对象或成功标识。

#### 3.2.7 接口
- 云函数：`user.main(event)`
- 前端页面：
  - `pages/profile/edit`
  - `pages/register/role`
  - `pages/register/student`
  - `pages/register/publisher`
  - `pages/student/mine`
  - `pages/publisher/mine`
  - `pages/admin/users`
  - `pages/admin/mine`
- 前端工具：
  - `utils/status-text.js`（`getRoleText/getVerifyStatusText/getJobStatusText`）

#### 3.2.8 限制条件
- 认证数据结构依赖 `verifyPayload` 与角色子字段并存（历史兼容）。

#### 3.2.9 存储分配
- 集合：`users`

#### 3.2.10 测试要点
- 角色切换后认证状态映射正确；
- 管理员审核后目标用户状态变化正确；
- 公开资料接口不泄露敏感字段。

---

### 3.3 钱包与工分模块

#### 3.3.1 性能
- 充值/提现为单用户单流水写入，性能稳定。

#### 3.3.2 输入项目
- `action`: `recharge/withdraw/walletLogs`
- `amount`（充值提现金额）

#### 3.3.3 功能
- 充值增加余额并写流水；
- 提现扣减余额并写流水；
- 查询钱包流水。

#### 3.3.4 输出项目
- `beforeBalance/afterBalance/amount`
- `list`（流水）

#### 3.3.5 算法
- 无特殊算法；余额校验 + 原子增减（`_.inc`）。

#### 3.3.6 程序逻辑
1. 校验金额；
2. 检查余额（提现）；
3. 更新 `users.pointsBalance`；
4. 新增 `wallet_logs`；
5. 返回最新余额。

#### 3.3.7 接口
- 云函数：`user`（钱包相关 action）
- 前端页面：`pages/wallet/index`、`pages/wallet/recharge`、`pages/wallet/withdraw`

#### 3.3.8 限制条件
- 当前支付渠道为 mock，不含真实支付网关回调。

#### 3.3.9 存储分配
- 集合：`users`、`wallet_logs`

#### 3.3.10 测试要点
- 充值大额边界；
- 提现余额不足；
- 流水顺序与余额一致性。

---

### 3.4 岗位发布与审核模块

#### 3.4.1 性能
- 岗位创建涉及押金扣减 + 岗位新增，性能中等但可接受。

#### 3.4.2 输入项目
- 发布参数：`title/category/location/timeDesc/description/contact/rewardPoints/needCount`
- 审核参数：`jobId/reason`

#### 3.4.3 功能
- 发布岗位并冻结发布者押金（总薪资 150%）。
- 管理员审核岗位通过/拒绝。
- 发布者查询自己岗位列表（含展示状态推导）。

#### 3.4.4 输出项目
- `jobId`、岗位列表 `list`
- 审核执行结果

#### 3.4.5 算法
- 押金计算算法：`publisherDepositTotal = ceil(totalReward * 1.5)`
- 学生押金基线：`ceil(rewardPoints * 0.5)`

#### 3.4.6 程序逻辑
1. 发布前校验字段与余额；
2. 扣押金；
3. 写入 `jobs` 为 `pending_review`；
4. 管理员审核后切换状态到 `open` 或 `rejected`。

#### 3.4.7 接口
- 云函数：
  - `job:createJob/myPublishedJobs/adminListPendingJobs/adminApproveJob/adminRejectJob`
- 页面：
  - 发布者：`pages/publisher/create`、`pages/publisher/jobs`
  - 管理员：`pages/admin/pending-jobs`

#### 3.4.8 限制条件
- 仅认证通过发布者可发布；
- 岗位状态迁移受 `JOB_STATUS_TRANSITIONS` 控制。

#### 3.4.9 存储分配
- 集合：`jobs`、`users`

#### 3.4.10 测试要点
- 押金不足发布失败；
- 审核通过后岗位进入大厅；
- 拒绝理由可回显。

---

### 3.5 报名与录用模块

#### 3.5.1 性能
- 报名去重 + 录用时多次查询，复杂度随岗位报名数增加。

#### 3.5.2 输入项目
- 报名：`jobId`
- 录用：`applicationId`

#### 3.5.3 功能
- 学生报名岗位（防重复参与同工单）。
- 发布者查看报名列表并录用学生。
- 录用时扣学生押金并创建工单。

#### 3.5.4 输出项目
- `applicationId/orderId`
- 报名列表（含学生资料）

#### 3.5.5 算法
- 去重策略：同 `jobId + studentOpenid` 报名与工单联合校验。
- 满员处理：达到 `needCount` 后批量拒绝剩余申请。

#### 3.5.6 程序逻辑
1. 学生报名 -> 查岗位状态 -> 去重 -> 写 `applications`；
2. 发布者录用 -> 校验权限/状态/余额 -> 扣押金 -> 创建 `work_orders`；
3. 根据活动工单数量判断是否满员并处理剩余申请。

#### 3.5.7 接口
- 云函数：`job:applyJob/listApplications/acceptApplication`
- 页面：`pages/student/jobs`、`pages/publisher/job-detail`

#### 3.5.8 限制条件
- 仅认证通过学生可报名；
- 仅岗位发布者可录用。

#### 3.5.9 存储分配
- 集合：`applications`、`work_orders`、`users`、`jobs`

#### 3.5.10 测试要点
- 重复报名拦截；
- 录用人数满员边界；
- 学生押金不足提示。

---

### 3.6 工单执行与完工提交模块

#### 3.6.1 性能
- 核心为单工单读写 + 少量关联写，实时性良好。

#### 3.6.2 输入项目
- 生成签到码：`orderId`
- 签到：`orderId/signCode`
- 完工提交：`orderId/photoFileId/text`
- 确认结算：`orderId`

#### 3.6.3 功能
- 发布者生成有效期签到码；
- 学生输入签到码完成签到；
- 学生上传完工图+文字；
- 发布者/管理员确认完工并结算发放工分。

#### 3.6.4 输出项目
- 签到码与过期时间
- 签到结果
- 提交结果
- 结算结果

#### 3.6.5 算法
- 签到码生成：6 位随机数。
- 状态迁移：`ORDER_STATUS_TRANSITIONS` 限制合法流转。
- 结算算法：
  - 学生到账：`reward + studentDeposit`
  - 发布者返还：`publisherPerOrderDeposit - reward`

#### 3.6.6 程序逻辑
1. 生成签到码写入工单（含过期时间）；
2. 签到校验工单归属、状态、码正确性、过期时间；
3. 完工提交校验文字和图片；
4. 结算校验纠纷待处理状态；
5. 更新工单、用户余额、岗位统计。

#### 3.6.7 接口
- 云函数：`generateSignCode.main`、`signIn.main`、`job:submitWork/confirmWork/myWorkOrders/publisherWorkOrders`
- 页面：`pages/student/orders`、`pages/work/submit`、`pages/publisher/job-detail`

#### 3.6.8 限制条件
- 完工提交必须由工单所属学生发起；
- 未处理纠纷存在时禁止确认完工。

#### 3.6.9 存储分配
- 集合：`work_orders`、`users`、`jobs`
- 云存储：`work-proof/**`

#### 3.6.10 测试要点
- 签到码错误/过期；
- 已签到重复签到；
- 提交完工后状态与按钮显示；
- 结算后余额与押金回退正确。

---

### 3.7 纠纷申诉与处理模块

#### 3.7.1 性能
- 纠纷列表涉及关联查询（订单、岗位、用户），管理端列表性能中等。

#### 3.7.2 输入项目
- 学生申诉：`orderId/reason/photoFileId?`
- 发布者爽约投诉：`orderId/reason`
- 其他问题投诉：`orderId/reason`
- 管理处理：`disputeId/approve/adminNote`

#### 3.7.3 功能
- 发起纠纷（拒不付薪、爽约、其他问题）；
- 管理员受理并执行接受/驳回；
- 处理完成后状态统一展示为“已完成”；
- 支持在订单详情中跳转定位到对应纠纷处理项。

#### 3.7.4 输出项目
- 纠纷列表（含投诉方、被投诉方、订单摘要、证据图）
- 处理结果提示

#### 3.7.5 算法
- 无复杂算法；核心为状态机与业务分支：
  - `no_show` 接受时触发爽约结算
  - `non_payment` 接受时触发强制结算
  - 其他投诉接受/驳回仅写处理结果

#### 3.7.6 程序逻辑
1. 发起纠纷前校验角色、工单归属与状态；
2. 校验是否已有 `pending` 纠纷；
3. 写入 `disputes`；
4. 管理员处理时根据 type 执行对应逻辑；
5. 更新纠纷状态并记录备注与处理人。

#### 3.7.7 接口
- 云函数：
  - `job:reportNoShow/reportMerchantNoPay/reportOtherDispute`
  - `job:adminListPendingDisputes/adminResolveDispute/adminCountPendingDisputes`
- 页面：
  - 发起：`pages/student/orders`、`pages/publisher/job-detail`
  - 处理：`pages/admin/disputes`
  - 入口：`pages/admin/order-detail`

#### 3.7.8 限制条件
- 同一订单同时只允许一条待处理纠纷；
- 一些处理依赖订单状态与签到状态。

#### 3.7.9 存储分配
- 集合：`disputes`、`work_orders`、`order_reports`、`users`、`jobs`
- 云存储：`appeal-proof/**`

#### 3.7.10 测试要点
- 重复发起纠纷拦截；
- 接受/驳回后状态展示“已完成”；
- 证据图片上传可选逻辑；
- 订单详情“去处理”跳转定位。

---

### 3.8 订单管理模块

#### 3.8.1 性能
- 管理员订单列表会聚合用户、岗位、纠纷计数，复杂度中等。

#### 3.8.2 输入项目
- 列表筛选：`adminPhase`（含 `dispute`）
- 详情查询：`orderId`

#### 3.8.3 功能
- 管理员查看所有工单；
- 四态/申诉标签筛选；
- 查看订单详情（参与方、完工提交、互评、纠纷记录）。

#### 3.8.4 输出项目
- `adminListAllWorkOrders`：订单摘要列表 + 申诉订单计数
- `adminGetWorkOrderDetail`：订单完整详情对象

#### 3.8.5 算法
- 管理员显示态推导：`deriveAdminOrderPhase(order)`
- 申诉标签计数：对 `disputes(status=pending)` 去重 `orderId`。

#### 3.8.6 程序逻辑
1. 拉取工单集合；
2. 结合纠纷集合计算申诉标签集合；
3. 按筛选条件过滤；
4. 聚合岗位与用户昵称；
5. 返回前端渲染数据。

#### 3.8.7 接口
- 云函数：`job:adminListAllWorkOrders/adminGetWorkOrderDetail`
- 页面：`pages/admin/orders`、`pages/admin/order-detail`

#### 3.8.8 限制条件
- 仅管理员角色可访问；
- 详情页对旧版云函数提供兼容回退加载。

#### 3.8.9 存储分配
- 集合：`work_orders`、`jobs`、`users`、`disputes`

#### 3.8.10 测试要点
- 各筛选标签返回正确；
- 申诉计数准确；
- 详情页纠纷、证据与互评展示完整。

---

### 3.9 聊天通信模块

#### 3.9.1 性能
- 会话列表通过合并收发消息并归并键，消息量大时有一定计算开销。

#### 3.9.2 输入项目
- 发消息：`jobId/toOpenid/text`
- 拉消息：`jobId/peerOpenid`

#### 3.9.3 功能
- 同岗位上下文的一对一聊天；
- 会话列表聚合；
- 消息按时间排序与分隔展示。

#### 3.9.4 输出项目
- `listMessages`：消息数组
- `listMyChats`：会话摘要数组（含对方用户与岗位信息）

#### 3.9.5 算法
- 会话归并键算法：`jobId + "__" + peerOpenid`
- 新旧消息比较：以 `createTime` 取最新摘要。

#### 3.9.6 程序逻辑
1. 写消息时校验参数与目标用户；
2. 读消息时双向拉取并合并排序；
3. 会话列表按归并键压缩；
4. 前端按昵称和岗位名渲染。

#### 3.9.7 接口
- 云函数：`job:sendMessage/listMessages/listMyChats`
- 页面：`pages/student/contact`、`pages/publisher/contact`、`pages/chat/detail`

#### 3.9.8 限制条件
- 不支持群聊；
- 每次查询有 `limit` 上限。

#### 3.9.9 存储分配
- 集合：`messages`

#### 3.9.10 测试要点
- 消息双向可见；
- 会话归并不串房间；
- 标题昵称显示与时间格式正确。

---

### 3.10 评价与信用模块

#### 3.10.1 性能
- 单次评价涉及工单更新 + 信用日志写入，开销低。

#### 3.10.2 输入项目
- `orderId/toRole/score/tags/content`

#### 3.10.3 功能
- 订单完成后互评；
- 根据评分与标签计算信用增减；
- 写入 `credit_logs`。

#### 3.10.4 输出项目
- 评价提交结果
- `delta`（信用变化值）

#### 3.10.5 算法
- 信用增减算法：`calcCreditDelta(score, tags)`，并限幅 [-5, 5]。

#### 3.10.6 程序逻辑
1. 校验订单状态为 `completed`；
2. 校验评价方身份与方向；
3. 防重复评价；
4. 写入评价字段；
5. 更新目标用户信用分并记录日志。

#### 3.10.7 接口
- 云函数：`job:submitReview`
- 页面：`pages/student/orders`、`pages/publisher/job-detail`、`pages/admin/order-detail`（展示）

#### 3.10.8 限制条件
- 仅订单双方可评价；
- 仅完成订单可评价。

#### 3.10.9 存储分配
- 集合：`work_orders`、`users`、`credit_logs`

#### 3.10.10 测试要点
- 重复评价拦截；
- 标签边界与评分边界；
- 信用值不小于 0。

---

### 3.11 管理员审核中心聚合模块

#### 3.11.1 性能
- 页面仅做入口聚合，性能压力小。

#### 3.11.2 输入项目
- 无复杂输入；主要为入口点击与统计刷新。

#### 3.11.3 功能
- 提供管理入口：
  - 审核用户资料
  - 审核待上架岗位
  - 订单管理

#### 3.11.4 输出项目
- 待审核岗位数量（按钮角标）

#### 3.11.5 算法
- 无特殊算法。

#### 3.11.6 程序逻辑
1. 页面展示时读取待审核岗位数；
2. 点击按钮跳转对应子页面。

#### 3.11.7 接口
- 前端页面：`pages/admin/audit`
- 依赖云函数：`job:adminListPendingJobs`

#### 3.11.8 限制条件
- 该页面需管理员身份访问，权限由后端接口实际控制。

#### 3.11.9 存储分配
- 无独立存储；仅读取 `jobs`。

#### 3.11.10 测试要点
- 按钮跳转路径正确；
- 角标数量刷新正确；
- 子页面返回不形成多层循环。

---

### 3.12 推荐岗位与定位模块

#### 3.12.1 性能
- 推荐接口默认最多拉取并评分 200 条岗位，分页返回，性能受岗位总量与距离计算方式影响。

#### 3.12.2 输入项目
- 推荐：`page/pageSize/location/expectedSalary/freeTimeSlots`
- 地点：经纬度与地址文本（由前端位置选择器提供）

#### 3.12.3 功能
- 对已认证学生提供岗位推荐列表；
- 按距离、薪资、时间匹配三维打分并排序；
- 支持使用腾讯地图距离接口（可配置）与 Haversine 兜底计算。

#### 3.12.4 输出项目
- `list`（含 `matchScore/distanceKm/scoreBreakdown`）
- `page/pageSize/total/hasMore`

#### 3.12.5 算法
- 综合评分：`distanceScore * 0.5 + salaryScore * 0.3 + timeScore * 0.2`
- 距离计算：优先第三方接口，失败回退球面距离算法。

#### 3.12.6 程序逻辑
1. 校验学生身份与认证状态；
2. 拉取可报名岗位并过滤已投递岗位；
3. 逐条计算距离/薪资/时间子分；
4. 汇总总分后排序并分页返回。

#### 3.12.7 接口
- 云函数：`job:getRecommendedJobs`
- 页面：`pages/student/recommend/recommend`、`pages/location-picker/location-picker`

#### 3.12.8 限制条件
- 仅认证通过的学生可使用推荐接口；
- 距离外部接口依赖环境变量地图密钥，缺失时自动降级本地算法。

#### 3.12.9 存储分配
- 推荐不新增持久化集合，依赖 `jobs/applications/users` 现有数据。

#### 3.12.10 测试要点
- 无定位时兜底推荐可用；
- 已投递岗位不重复推荐；
- 分页与 `hasMore` 正确；
- 外部地图接口失败时可自动回退。

---

## 附录 A：云函数清单（逐项）

| 云函数 | 主要职责 | 关键 action/方法 |
|---|---|---|
| `login` | 登录与用户初始化 | `exports.main` |
| `user` | 用户资料、认证、钱包、管理员审核用户 | `getProfile/getPublicProfile/updateProfile/recharge/withdraw/walletLogs/setRole/switchBackAdmin/submitVerify/adminListPendingUsers/adminApproveUser/adminRejectUser/adminFallbackToStudent` |
| `job` | 岗位、报名、录用、工单、纠纷、聊天、管理员订单 | 见 3.4~3.10 各 action |
| `generateSignCode` | 生成签到码 | `exports.main` |
| `signIn` | 学生签到 | `exports.main` |

## 附录 B：前端页面清单（逐项）

| 页面 | 作用 |
|---|---|
| `pages/login/login` | 登录入口 |
| `pages/index/index` | 首页与角色入口分流 |
| `pages/profile/edit` | 个人资料编辑 |
| `pages/register/role` | 角色选择 |
| `pages/register/student` | 兼职者认证申请 |
| `pages/register/publisher` | 发布者认证申请 |
| `pages/student/jobs` | 兼职岗位大厅 |
| `pages/student/recommend/recommend` | 推荐岗位列表（智能匹配） |
| `pages/student/contact` | 兼职者会话列表 |
| `pages/student/job-detail` | 兼职者查看岗位详情 |
| `pages/student/mine` | 兼职者个人中心 |
| `pages/student/orders` | 兼职者工单、签到、完工、申诉、评价 |
| `pages/publisher/create` | 发布岗位 |
| `pages/publisher/jobs` | 发布者岗位列表 |
| `pages/publisher/contact` | 发布者会话列表 |
| `pages/publisher/mine` | 发布者个人中心 |
| `pages/publisher/job-detail` | 发布者处理报名、工单、纠纷、评价 |
| `pages/work/submit` | 完工证明提交 |
| `pages/wallet/index` | 钱包总览 |
| `pages/wallet/recharge` | 充值 |
| `pages/wallet/withdraw` | 提现 |
| `pages/admin/audit` | 审核中心入口 |
| `pages/admin/users` | 审核用户资料 |
| `pages/admin/pending-jobs` | 审核待上架岗位 |
| `pages/admin/orders` | 订单管理与筛选 |
| `pages/admin/order-detail` | 订单详情与纠纷入口 |
| `pages/admin/disputes` | 纠纷处理（接受/驳回） |
| `pages/admin/mine` | 管理员个人中心 |
| `pages/chat/detail` | 聊天详情页 |
| `pages/location-picker/location-picker` | 地点选择与定位辅助页 |

---

## 附录 C：答辩版过程与接口总表

### C.1 核心流程时序（文字版）
```text
参与者：兼职者、发布者、管理员、云函数(job/user/login/signIn/generateSignCode)

1) 登录阶段
   小程序 -> login: 创建/补齐 users

2) 发布与审核
   发布者 -> job:createJob(扣押金, 状态 pending_review)
   管理员 -> job:adminApproveJob/adminRejectJob

3) 报名与录用
   兼职者 -> job:applyJob
   发布者 -> job:listApplications -> job:acceptApplication(扣学生押金, 生成工单)

4) 履约与结算
   发布者 -> generateSignCode
   兼职者 -> signIn
   兼职者 -> job:submitWork(图+文)
   发布者/管理员 -> job:confirmWork(结算+返还押金+信用更新)

5) 异常处理
   任一方 -> job:report*
   管理员 -> job:adminResolveDispute
```

### C.2 关键状态迁移矩阵（实现对照）

#### C.2.1 岗位状态（`jobs.status`）

| 当前状态 | 可迁移状态 | 触发动作 |
|---|---|---|
| `pending_review` | `open` / `rejected` | `adminApproveJob` / `adminRejectJob` |
| `open` | `ongoing` / `closed` / `publisher_cancelled` / `admin_cancelled` | `acceptApplication` / 交易收敛 / 取消交易 |
| `approved`（历史兼容） | `ongoing` / `closed` / `publisher_cancelled` / `admin_cancelled` | 与 `open` 同口径 |
| `ongoing` | `closed` / `publisher_cancelled` / `admin_cancelled` | 结算完成 / 主动下架 / 管理取消 |

#### C.2.2 工单状态（`work_orders.status`）

| 当前状态 | 可迁移状态 | 触发动作 |
|---|---|---|
| `ongoing` | `pending_settlement` / `submitted` / `completed` / `closed` / `publisher_cancelled` / `admin_cancelled` | `submitWork` / 结算 / 爽约/取消 |
| `pending_settlement` | `completed` / `closed` / `publisher_cancelled` / `admin_cancelled` | `confirmWork` / 纠纷处理/取消 |
| `submitted` | `pending_settlement` / `completed` / `closed` / `publisher_cancelled` / `admin_cancelled` | 兼容迁移 |

#### C.2.3 纠纷状态（`disputes.status`）

| 当前状态 | 可迁移状态 | 处理动作 |
|---|---|---|
| `pending` | `resolved` | `adminResolveDispute` |

### C.3 接口分层汇总表（前端->云函数）

| 分层 | 典型接口 | 关键输入 | 关键输出 |
|---|---|---|---|
| 认证与角色层 | `login.main`、`user:setRole`、`user:submitVerify` | `userInfo/role/payload` | `user`、认证状态 |
| 岗位层 | `job:createJob`、`job:listOpenJobs`、`job:getRecommendedJobs` | 岗位字段/筛选参数 | 岗位列表、推荐分数 |
| 交易层 | `job:applyJob`、`job:acceptApplication`、`job:submitWork`、`job:confirmWork` | `jobId/applicationId/orderId` | `applicationId/orderId`、结算结果 |
| 纠纷层 | `job:report*`、`job:adminResolveDispute` | `orderId/disputeId/reason` | 处理结果、收敛状态 |
| 管理层 | `job:adminList*`、`user:adminListPendingUsers` | 筛选条件 | 审核/订单/纠纷列表 |
| 交互层 | `job:sendMessage/listMessages/listMyChats` | `jobId/toOpenid/text` | 会话/消息列表 |

### C.4 答辩展示建议口径
- 技术亮点 1：状态机约束（岗位、工单、纠纷）保障业务一致性；
- 技术亮点 2：押金与结算在云函数集中处理，避免前端篡改；
- 技术亮点 3：展示层中文映射与存储层英文状态解耦，便于兼容与扩展；
- 技术亮点 4：推荐模块采用“距离+薪资+时间”综合评分并支持第三方距离接口降级。

